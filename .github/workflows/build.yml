name: Build OfflineCatalogApp with Theos

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: macos-latest

    env:
      THEOS: /tmp/theos
      PATH: /tmp/theos/bin:${{ env.PATH }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          brew install ldid dpkg make unzip zip || true
          # clang er allerede installeret på macos-latest runner

      - name: Clone Theos if not present
        run: |
          if [ ! -d "${{ env.THEOS }}" ]; then
            git clone --depth=1 https://github.com/theos/theos.git "${{ env.THEOS }}"
          else
            echo "Theos already exists"
          fi

      - name: Build tweak with Theos
        shell: bash
        run: |
          source "${{ env.THEOS }}/setup.sh"
          make clean
          make

      - name: Package IPA
        run: ./build.sh

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: OfflineCatalogApp.ipa
          path: OfflineCatalogApp.ipa
